#ifndef ARRAY_GEN_H
#define ARRAY_GEN_H

#include <stdlib.h>
#include <stdio.h>
#include "../main.h"

    void swap(ArrElement *arr, ArrElement *n);                    // modify the array (shuffles it)

#endif